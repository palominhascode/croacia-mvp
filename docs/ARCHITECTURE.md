# 🏗️ Architecture

## Overview

```
┌─────────────────────────────────────────┐
│         HTTP Request (Port 8080)        │
├─────────────────────────────────────────┤
│            main.nim (Server)            │
│      - HTTP server                      │
│      - Request routing                  │
├─────────────────────────────────────────┤
│          scraper.nim (Core)             │
│      - URL normalization                │
│      - HTTP client calls                │
│      - HTML fetching                    │
├─────────────────────────────────────────┤
│     cloudscraper_bridge.nim (Bridge)    │
│      - Python subprocess                │
│      - Cloudflare bypass                │
├─────────────────────────────────────────┤
│       cache_manager.nim (Utils)         │
│      - Response caching                 │
│      - TTL management                   │
└─────────────────────────────────────────┘
```

## Data Flow

1. Request → main.nim
2. Validate → scraper.nim
3. Cache check → cache_manager.nim
4. HTTP request → Success
5. If blocked → cloudscraper_bridge.nim
6. Cache result → Return response
