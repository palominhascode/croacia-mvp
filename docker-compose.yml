version: '3.8'

services:
  croacia:
    build: .
    ports:
      - "8080:8080"
    environment:
      - SERPER_API_KEY=${SERPER_API_KEY}
      - PORT=8080
      - BIND_ADDR=0.0.0.0
    volumes:
      - ./src:/app/src
    command: nim c -r src/main.nim
